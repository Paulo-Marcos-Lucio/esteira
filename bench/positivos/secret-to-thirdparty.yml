name: segredo-para-terceiro
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  publica:
    runs-on: ubuntu-latest
    steps:
      - uses: fastly/dev-hub-action@v2
        with:
          token: ${{ secrets.DEPLOY_TOKEN }}
