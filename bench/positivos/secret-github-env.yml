name: segredo-no-ambiente
on:
  push:
    branches: [main]
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "API_KEY=${{ secrets.API_KEY }}" >> $GITHUB_ENV
